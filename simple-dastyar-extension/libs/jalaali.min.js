// --- Minimal Jalaali Conversion Functions (Fixed Version) ---
const jalaali = {
  toJalaali: function (gy, gm, gd) {
    const g_d_m = [0,31,59,90,120,151,181,212,243,273,304,334];
    let jy = (gy <= 1600) ? 0 : 979;
    gy -= (gy <= 1600) ? 621 : 1600;
    const gy2 = (gm > 2) ? (gy + 1) : gy;
    let days = (365 * gy) + Math.floor((gy2 + 3) / 4) - Math.floor((gy2 + 99) / 100) +
               Math.floor((gy2 + 399) / 400) - 80 + gd + g_d_m[gm - 1];
    jy += 33 * Math.floor(days / 12053);
    days %= 12053;
    jy += 4 * Math.floor(days / 1461);
    days %= 1461;
    jy += Math.floor((days - 1) / 365);
    if (days > 365) days = (days - 1) % 365;
    const jm = (days < 186) ? 1 + Math.floor(days / 31) : 7 + Math.floor((days - 186) / 30);
    const jd = 1 + ((days < 186) ? (days % 31) : ((days - 186) % 30));
    return { jy, jm, jd };
  },

  toGregorian: function (jy, jm, jd) {
    jy += 1595;
    const days = -355668 + (365 * jy) + Math.floor(jy / 33) * 8 + Math.floor(((jy % 33) + 3) / 4)
                + jd + ((jm < 7) ? (jm - 1) * 31 : ((jm - 7) * 30 + 186));
    let gy = 400 * Math.floor(days / 146097);
    let rem = days % 146097;
    if (rem > 36524) {
      gy += 100 * Math.floor(--rem / 36524);
      rem %= 36524;
      if (rem >= 365) rem++;
    }
    gy += 4 * Math.floor(rem / 1461);
    rem %= 1461;
    if (rem > 365) gy += Math.floor((rem - 1) / 365);
    let gd = (rem > 365) ? (rem - 1) % 365 : rem; //changed to let
    const sal_a = [
      0,
      31,
      ((gy % 4 === 0 && gy % 100 !== 0) || (gy % 400 === 0)) ? 29 : 28,
      31,30,31,30,31,31,30,31,30,31
    ];
    let gm;
    for (gm = 0; gm < 13 && gd >= sal_a[gm]; gm++) gd -= sal_a[gm];
    return { gy, gm, gd: gd + 1 };
  },

  jalaaliMonthLength: function (jy, jm) {
    if (jm <= 6) return 31;
    if (jm <= 11) return 30;
    return jalaali.isLeapJalaaliYear(jy) ? 30 : 29;
  },

  isLeapJalaaliYear: function (jy) {
    return (((jy - (jy > 0 ? 474 : 473)) % 2820 + 474 + 38) * 682) % 2816 < 682;
  }
};
